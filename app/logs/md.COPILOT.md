

## Appended conversation: requested per-edge tests and commit

```
User requested that every distinct edge case be a separate @Test method. Assistant proposed a new AppTest where each normal case, edge case, exception case, and null check is its own test. The user confirmed and the tests were updated accordingly.
```
